{
  "name": "la-verdad-incomoda-android",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check analyze.js && node --check extract-content.js && node --check health.js && node --check translate-ui.js"
  },
  "engines": {
    "node": ">=20"
  }
}
