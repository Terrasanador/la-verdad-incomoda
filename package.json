{
  "name": "la-verdad-incomoda-android",
  "version": "1.6.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check analyze.js && node --check extract-content.js && node --check content-api.js && node --check health.js && node --check translate-ui.js"
  },
  "engines": {
    "node": ">=20"
  }
}
